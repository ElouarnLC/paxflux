/**
 * Version this client reports to the server (device pairing, action
 * batches, heartbeats). Kept in one place so the three call sites can never
 * disagree about what the supervisor sees for a given device.
 */
export const CLIENT_APP_VERSION = '1.0.0';
